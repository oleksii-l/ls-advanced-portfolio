.header {
    
    min-height: 150px;
    position: fixed;
    width: 100%;
    z-index: 2;
}

.header__container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 36px;
    font-weight: 800;
}